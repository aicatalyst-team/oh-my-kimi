apiVersion: v1
kind: Service
metadata:
  name: omk
  namespace: poc-oh-my-kimi
  labels:
    app: omk
    autopoc.io/project: oh-my-kimi
spec:
  type: ClusterIP
  selector:
    app: omk
  ports:
    - port: 8080
      targetPort: 8080
